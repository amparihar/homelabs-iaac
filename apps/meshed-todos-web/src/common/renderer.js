import { useRef } from 'react';

const renderCounter = (component = '') => {
  // const renderer = useRef(1);
  // console.log(`Rendered ${component} ${renderer.current++} times.`);
};

export const useRenderer = renderCounter;
